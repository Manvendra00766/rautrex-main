[build]
builder = "nixpacks"
[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1 --timeout-keep-alive 300"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
